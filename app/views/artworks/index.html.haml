- cache([:artwork, page]) do
  %h1 Electric Objects
  %p.lead Access a rich library of works made just for EO1. Browse collections curated by prominent artists, publishers, libraries, and museums, and keep anything you like. Display your own GIFs, pics, video, and web art, too.
  .container-fluid= render(@artworks)
  %nav.text-center
    %ul.pagination.pagination-lg
      %li{class: (:disabled unless page > 1)}
        = link_to('Previous', artworks_page_path(-1))
      - pagination_buttons.each do |p|
        %li.hidden-xs{class: (:active if page == p)}
          = link_to(p, artworks_path(p))
      %li= link_to('Next', artworks_page_path(+1))
